type UnsplashImageType = {
    description: string
    urls: {
        full: string
        raw: string
        regular: string
        small: string
        small_s3: string
        thubm: string
    }
}

export type {
    UnsplashImageType
}